# Railway 环境变量配置

# ===== 数据库配置 (Railway MySQL 插件会自动提供) =====
# 在 Railway Dashboard 中添加 MySQL 插件后，以下变量会自动注入
# 无需手动设置，除非使用外部数据库

# DATABASE_URL=${{MySQL.DATABASE_URL}}
# MYSQL_HOST=${{MySQL.HOST}}
# MYSQL_PORT=${{MySQL.PORT}}
# MYSQL_USER=${{MySQL.USERNAME}}
# MYSQL_PASSWORD=${{MySQL.PASSWORD}}
# MYSQL_DATABASE=${{MySQL.DATABASE}}

# 如果需要手动配置 MySQL：
MYSQL_HOST=
MYSQL_PORT=3306
MYSQL_DATABASE=healthcare_admin
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

# ===== Redis 配置 (Railway Redis 插件会自动提供) =====
# REDIS_URL=${{Redis.REDIS_URL}}
# REDIS_HOST=${{Redis.HOST}}
# REDIS_PORT=${{Redis.PORT}}
# REDIS_PASSWORD=${{Redis.PASSWORD}}

# 如果需要手动配置 Redis：
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# ===== JWT 配置 =====
# 生成随机密钥：openssl rand -base64 32
JWT_SECRET=

# ===== 企业微信配置 (必填) =====
# 从企业微信后台获取：https://work.weixin.qq.com/
WECHAT_CORP_ID=
WECHAT_CORP_SECRET=
WECHAT_AGENT_ID=
WECHAT_TOKEN=
WECHAT_ENCODING_AES_KEY=

ENCRYPTION_KEY=

APP_CORS_ALLOWED_ORIGIN_PATTERNS=

# ===== 应用配置 =====
NODE_ENV=production
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080
PORT=8080

# ===== 日志配置 =====
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_HEALTHCARE=DEBUG

# ===== 其他可选配置 =====
# 文件上传限制
SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=10MB
SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=20MB

# JWT 过期时间（毫秒）
JWT_ACCESS_TOKEN_EXPIRATION=7200000
JWT_REFRESH_TOKEN_EXPIRATION=604800000
