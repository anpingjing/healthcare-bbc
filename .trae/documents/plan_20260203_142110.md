## 1. 移除 Docker 依赖 (Refactor to non-Docker)
- **删除文件**: 删除 [docker-compose.yml](file:///d:/traecn/企业微信项目/healthcare-bbc/docker-compose.yml)。
- **更新文档**: 
    - 修改 [README.md](file:///d:/traecn/企业微信项目/healthcare-bbc/README.md)，移除 Docker 相关步骤。
    - 在 [README.md](file:///d:/traecn/企业微信项目/healthcare-bbc/README.md) 中增加手动安装 MySQL 8.0 和 Redis 6.0 的指引，并说明如何导入 [schema.sql](file:///d:/traecn/企业微信项目/healthcare-bbc/deploy/sql/schema.sql)。
    - 更新 [PROJECT_PLAN.md](file:///d:/traecn/企业微信项目/healthcare-bbc/PROJECT_PLAN.md)，将“基础开发环境搭建 (Docker)”改为“本地开发环境配置”。

## 2. 后端基础框架开发 (Backend Foundation)
- **依赖升级**: 将 `mybatis-spring-boot-starter` 替换为 `mybatis-plus-boot-starter` (v3.5.3.1)，提升开发效率。
- **通用组件**:
    - 创建 `com.healthcare.bbc.common.Result`: 统一 API 响应格式。
    - 创建 `com.healthcare.bbc.exception.GlobalExceptionHandler`: 全局异常处理，捕获业务及系统异常。
- **配置优化**: 
    - 完善 [application.yml](file:///d:/traecn/企业微信项目/healthcare-bbc/backend/src/main/resources/application.yml) 中的数据库连接与 MyBatis-Plus 配置。
    - 启用 Swagger (SpringDoc) 接口文档，方便前后端联调。

## 3. 用户模块初步开发 (User Module Development)
- **实体层 (Entity)**: 创建 `User.java` 对应 `user_info` 表。
- **持久层 (Mapper)**: 创建 `UserMapper.java`。
- **业务层 (Service)**: 实现 `UserService` 及其实现类，包含基础的用户查询逻辑。
- **控制层 (Controller)**: 创建 `UserController.java`，提供根据 ID 获取用户信息等基础 API。

## 4. 前端基础架构搭建 (Frontend Foundation)
- **依赖安装**: 安装 `axios`, `vue-router`, `pinia`, `element-plus` 等核心依赖。
- **目录规范**: 建立 `api`, `utils`, `views`, `store`, `layout` 等标准目录结构。
- **网络请求**: 封装 `src/utils/request.ts`，基于 Axios 实现统一的拦截器（处理 Token、错误提示等）。
- **路由与状态**:
    - 配置 `vue-router` 基础路由（首页、用户采集页）。
    - 初始化 `pinia` 存储用户信息及企微状态。
- **UI 组件**: 全局引入 `Element Plus` 并配置暗色模式/主题。

## 5. 验证与联调
- 启动后端服务，通过 Swagger 页面验证用户 API 是否可用。
- 启动前端服务，通过 Axios 尝试调用后端接口，验证跨域及响应拦截。
